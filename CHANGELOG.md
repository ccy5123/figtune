# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [1.0.0] — 미공개

첫 정식 릴리스. **리눅스에서만 검증했다** — CI가 ubuntu 단독이므로 다른 OS는
"아마 돌지만 아무도 확인하지 않았다"가 정직한 상태다. macOS 지원은 v3 예정.

### 추가

- 캔버스에서 Shift 또는 Ctrl 클릭으로 여러 요소를 함께 고른다. PowerPoint와
  같이 두 키는 같게 동작한다(토글-추가). 범위 선택은 트리가 맡는다.
- 여럿을 고르면 인스펙터가 공통 속성만 보여주고, 고친 값은 고른 것 전부에
  들어간다. 값이 갈리는 칸은 비워 둔다.
- 고른 것들을 캔버스에서 통째로 끌어 옮긴다.
- 축 본체를 끌어 옮긴다. 이전에는 고른 뒤 핸들로 크기만 바꿀 수 있었다.
- 종이 맞추기가 정해진 횟수 안에 수렴하지 못하면 상태줄로 알린다.
- 데이터 점이 0개인 artist(seaborn hue 범례용)를 트리에서 `(범례 전용)`으로
  표시한다. 색을 바꿔도 화면에 아무 일이 없는 이유를 알 수 있게 한다.

### 수정

- figtune이 추가한 텍스트의 생성·삭제·이동이 실행 취소에 실린다. 이전에는
  히스토리에 아무것도 남지 않아, Ctrl+Z가 그것을 건너뛰고 그 전 편집을
  되돌렸다.
- 패널 라벨 `(a)(b)(c)` 일괄 삽입이 패널 수와 무관하게 실행 취소 한 칸이다.
- usertext 끌기가 다른 끌기와 같은 경로를 거친다. 잡은 지점 보정이 없어서
  글자 끝을 잡으면 앵커가 커서 밑으로 순간이동하던 것을 고쳤다.
- `--python` 서브프로세스 모드에서도 기본 글꼴이 적용된다. rcParams는
  프로세스 경계를 넘지 않아 실행 모드에 따라 다른 글꼴이 나왔다.
- 범례 `loc`과 텍스트 좌표가 같은 `위치` 라벨을 쓰던 것을 나눴다. 영어로는
  둘 다 "Position"이 되어 구분이 사라졌다.
- 확대·이동 모드에서 편집 제스처가 함께 처리되지 않는다. 관찰 도구가 spec을
  건드리는 경로를 막았다.

### 범위에서 뺀 것

- 스타일 프로파일 저장·재적용. spec의 키가 위치 기반(`ax0.line0`)이라 다른
  그림에 그대로 얹으면 엉뚱한 계열에 서식이 붙는다. 옮길 수 있는 부분만
  담으려면 별도의 패턴 문법이 필요한데, 그만한 값을 하지 않는다고 판단했다.

### 알려진 한계

- PowerPoint VBA 애드인은 실행 검증된 적이 없다(리눅스에서 작성).
- 벡터 SVG가 PowerPoint에서 실제로 벡터로 렌더되는지 확인하지 못했다.
  기본값은 PNG다.
- 배치를 처음 끌면 스크립트가 남긴 여백이 `layout.MARGIN`(0.1in)으로 다시
  잡힌다. "종이 = 내용의 경계 + 여백" 규칙을 하나로 유지한 대가다.
- 미니 툴바를 다시 부르려면 캔버스를 다시 클릭해야 한다.
