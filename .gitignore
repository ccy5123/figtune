__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/

# figtune 산출물 (원본 스크립트 옆에 생긴다)
*.figtune.yaml
*_style.py
*_norm.py

# 병합 결과. 이름은 사용자가 정하므로 예제 폴더에서 시험할 때 쓰는 이름만
# 막는다 — 한 번 실수로 커밋된 적이 있다.
examples/merged*.py
